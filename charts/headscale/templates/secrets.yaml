apiVersion: v1
stringData:
  token: ""
kind: Secret
type: Opaque
metadata:
  name: "{{ include "headscale.fullname" . }}-token"
  labels:
    headscale: token
    cluster: {{ .Release.Name }}
    {{- include "k3s-server.labels" . | nindent 4 }}